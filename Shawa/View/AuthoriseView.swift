//
//  AuthoriseView.swift
//  Shawa
//
//  Created by Alex on 10.03.23.
//

import SwiftUI

struct AuthoriseView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
        .padding()
    }
}

struct AuthoriseView_Previews: PreviewProvider {
    static var previews: some View {
        AuthoriseView()
    }
}
